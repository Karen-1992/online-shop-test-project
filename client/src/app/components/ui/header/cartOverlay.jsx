import React from "react";

const CartOverlay = () => {
    return (
        <div className="cart-overlay cart-shadow">
            My Bag. X items
        </div>
    );
};

export default CartOverlay;
