import React, {useState} from "react";

const Cart = () => {
    // cart
    const [cartQuantity, setCartQuantity] = useState(0);

    return (
        <h1>Cart Page</h1>
    );
};

export default Cart;